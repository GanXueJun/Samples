#one config per line
#use '=' to separate key and value

#chip config
device_id = 0 #use the device to run the program

#yolov3 model path
model_path = ./data/models/yolov3/yolov3_aipp.om

#yolov3 model input width and height
model_width = 416
model_height = 416

model_encrypted = false #Declares whether the model is encrypted

#NPU ID
card_id = 0 #Only need to be set when the model is encrypted